---
import typograf from '@utils/typograf';

interface Props {
    image: string;
    name: string;
    text: string;
}

const { image, name, text } = Astro.props;
---
<article class="item-ingredient">
    <img
        class="item-ingredient__image lazy"
        data-src={`${image}@1x.webp`}
        data-srcset={`${image}@2x.webp 2x`}
        alt={name}
    />
    <div class="item-ingredient__body">
        <h3 class="item-ingredient__name" set:html={typograf(name)} />
        <p class="item-ingredient__text" set:html={typograf(text)} />
    </div>
</article>
